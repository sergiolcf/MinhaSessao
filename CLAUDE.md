# MinhaSessao - Diretrizes do Projeto

## Visão Geral
Sistema de gestão de sessões de psicologia para Psicólogos e Pacientes.
Solução: SLCF_MinhaSessao.sln
Tech Stack: .NET 8 MVC, Entity Framework Core, SQLite, Bootstrap 5.

## Paleta de Cores & Layout
- Fundo Principal: Cinza Claro (#F8F9FA)
- Texto / Elementos Primários: Grafite (#2B2D42)
- Destaques / Botões de Ação: Laranja (#FF6B35)
- Layout: Limpo, acolhedor. Autenticação (Login/Cadastro) usa páginas dedicadas (`_LayoutAuth`), não modais; modais Bootstrap continuam usados dentro do Dashboard (ex.: cadastro de paciente, anotações).

## Comandos Principais
- Compilar Solução: dotnet build SLCF_MinhaSessao.sln
- Rodar localmente: dotnet run --project MinhaSessao.csproj
- Migrações EF Core: dotnet ef database update --project MinhaSessao.csproj

## Regras de Código
- Sempre mantenha os botões de confirmação/ação no canto inferior DIREITO dos modais.
- Um único formulário de login para ambos os perfis (identificação via DB) — hoje é a página `/Account/Login`, não mais um modal.
- Código e comentários em Português (PT-BR).
- Sempre validar alterações compilando a solução antes de finalizar tarefas.
- Toda action que lê/grava dados vinculados a um Profissional (Pacientes, Anotações, etc.) deve usar `[Authorize(Roles = AutenticacaoService.PapelProfissional)]` e obter o Id do profissional logado via `User.ObterProfissionalId()`; toda action exclusiva do Paciente usa `[Authorize(Roles = AutenticacaoService.PapelPaciente)]` e `User.ObterPacienteId()` — nunca confiar em um Id vindo de querystring, rota ou corpo da requisição para autorização.
- Operações de CRUD dentro do Dashboard (criar/editar/excluir/paginar/buscar) devem ser feitas via Fetch API (AJAX), sem recarregar a página inteira.

## Segurança & Dependências
- NUNCA instale pacotes NuGet, bibliotecas externas ou serviços de terceiros sem solicitar autorização prévia ao usuário.
- O projeto DEVE utilizar exclusivamente tecnologias e bibliotecas 100% gratuitas e open-source (sem custos de licença ou planos pagos).

## Arquitetura Implementada
Esta seção descreve o estado atual do projeto por módulo/domínio (não por card). Ao implementar um novo card que altera um módulo existente, atualize a seção correspondente em vez de adicionar uma seção nova.

### Estrutura de Models e Projeto
- Namespace raiz do projeto é `MinhaSessao` (não `SLCF_MinhaSessao` — este é só o nome do arquivo `.sln`).
- `Models/Entities/*` — entidades mapeadas pelo EF Core (`Id` como `Guid`, gerado via `Guid.NewGuid()`).
- `Models/ViewModels/*` — models de formulário/exibição, um por finalidade (criação, edição, listagem, detalhes).
- `Services/*` — lógica de domínio reutilizável (ex.: `AutenticacaoService`).
- `Extensions/*` — extension methods auxiliares (ex.: `ClaimsPrincipalExtensions`).
- Persistência via EF Core + SQLite: `Data/ApplicationDbContext.cs`, connection string em `appsettings.json` (`ConnectionStrings:DefaultConnection`), banco local `minhasessao.db` (gitignored).

### Autenticação (Cookie + Senha, Profissional e Paciente)
- Autenticação real por Cookie (`Program.cs`: `AddAuthentication().AddCookie()`, `LoginPath = /Account/Login`, expira em 7 dias com sliding expiration).
- Senha nunca é armazenada em texto puro: hash via `PasswordHasher<Profissional>`/`PasswordHasher<Paciente>` (`Microsoft.AspNetCore.Identity`, já incluso no SDK `Microsoft.NET.Sdk.Web`, sem pacote NuGet adicional).
- `Services/AutenticacaoService.cs` centraliza hash/verificação de senha (`HashSenha`/`VerificarSenha` para Profissional, `HashSenhaPaciente`/`VerificarSenhaPaciente` para Paciente) e a autenticação (`AutenticarProfissionalAsync`/`AutenticarPacienteAsync`, montam os Claims e chamam `SignInAsync`). Ambos os métodos de autenticação adicionam um Claim de papel (`ClaimTypes.Role`) com o valor de `AutenticacaoService.PapelProfissional` ou `PapelPaciente` — é isso que permite `[Authorize(Roles = "...")]` diferenciar as rotas de cada perfil.
- `Extensions/ClaimsPrincipalExtensions.cs`: `ObterProfissionalId()`/`ObterPacienteId()` extraem o Id do usuário logado a partir dos Claims — é assim que toda controller descobre "quem está logado" (cada uma só é chamada dentro de controllers já restritas ao papel correspondente).
- `Controllers/AccountController.cs`: `Login` (GET/POST) é o único formulário para os dois perfis — normaliza o e-mail com `.Trim().ToLower()`, procura primeiro em `Profissionais` e depois em `Pacientes`, autentica com o método correspondente e redireciona para `Dashboard` (Profissional) ou `PainelPaciente` (Paciente). `LoginSimuladoTeste` (atalho de desenvolvimento, loga automaticamente o primeiro profissional do banco), `Logout` (comum aos dois perfis).
- Views: `Views/Account/Login.cshtml` e `Views/Account/Cadastro.cshtml`, ambas com `Views/Shared/_LayoutAuth.cshtml` (layout minimalista, não usa o `_Layout.cshtml` público nem os layouts de dashboard).
- Ainda não implementado: troca de senha obrigatória no primeiro login e recuperação de senha. O Paciente pode ganhar acesso tanto pelo cadastro feito pelo Profissional (senha temporária, ver "Pacientes" abaixo) quanto por autocadastro próprio (ver "Cadastro (Profissional e Paciente)" abaixo).

### Cadastro (Profissional e Paciente)
- Tela única `Views/Account/Cadastro.cshtml` (`_LayoutAuth`), com abas Bootstrap ("Sou Profissional" / "Sou Paciente") — substituiu o antigo modal de cadastro do Profissional na landing page e a rota solta `/Paciente/Cadastrar`. Cada aba é um `<form>` independente que posta para uma action diferente do `AccountController`; os campos usam nomes simples (`NomeCompleto`, `Email`, `Senha`...), sem prefixo `Model.Profissional.X`/`Model.Paciente.X`, porque cada action recebe seu próprio ViewModel (`ProfissionalViewModel`/`PacienteCadastroViewModel`) direto como parâmetro — os campos de cada aba vêm de um `<partial>` próprio (`_CamposCadastroProfissional.cshtml`/`_CamposCadastroPaciente.cshtml`) com `@model` do tipo específico, exatamente para gerar esses nomes sem prefixo.
- `Models/ViewModels/CadastroViewModel.cs` só existe para renderizar a tela (`PerfilAtivo` + os dois sub-ViewModels vazios ou repopulados após erro) — nunca é o tipo postado por um form.
- `Controllers/AccountController.cs`:
  - `[HttpGet] Cadastro(string? perfil)` — monta a tela; `PerfilAtivo` vem da querystring (`?perfil=paciente`), default `"profissional"`.
  - `[HttpPost] CadastroProfissional(ProfissionalViewModel model)` — mesma lógica que existia em `ProfissionalController.Criar` (upload de foto opcional para `wwwroot/uploads/profissionais/`, hash da senha, autologin), mas retornando `View`/`RedirectToAction` em vez de `Json` (form MVC tradicional, mesmo padrão do `Login`). Sucesso → `RedirectToAction("Index", "Dashboard")`, direto, sem modal de sucesso.
  - `[HttpPost] CadastroPaciente(PacienteCadastroViewModel model)` — mesma lógica que existia em `PacienteController.Cadastrar` (checagem de e-mail duplicado, hash da senha, autologin). Sucesso → `RedirectToAction("Index", "PainelPaciente")`. **Não cria nenhum `VinculoPacienteProfissional`** — vínculo continua sendo criado só pelo Profissional (ver "Pacientes"/"Vínculo Paciente-Profissional").
  - Em erro (validação ou e-mail duplicado), cada action re-renderiza `View("Cadastro", ...)` com `PerfilAtivo` na aba correta e os dados já digitados preservados.
- `wwwroot/js/cadastro.js` — só o contador de caracteres da Apresentação do Profissional (migrado do antigo `site.js`, que não tem mais nada de cadastro).

### Dashboard
- `Views/Shared/_LayoutDashboard.cshtml`: layout do painel interno, navbar/sidebar em grafite (`#2B2D42`) com destaques em laranja (`#FF6B35`), Bootstrap Icons via CDN.
- Sidebar retrátil (botão hambúrguer alterna `.sidebar-collapsed`), estado persistido em `localStorage` (`wwwroot/js/dashboard.js`); item ativo do menu calculado dinamicamente pela rota atual (`ViewContext.RouteData`), nunca fixo no HTML.
- "Minha Agenda", "Configurações" e "Minhas Sessões" já são reais (ver seções "Agenda (Painel do Profissional)", "Configurações (Profissional)" e "Sessões (Painel do Profissional)" abaixo).
- Estilos/scripts isolados em `wwwroot/css/dashboard.css` e `wwwroot/js/dashboard.js` (não compartilham `site.css`/`site.js` da landing page).
- **Tela inicial (`Views/Dashboard/Index.cshtml`, `DashboardController.Index`) é o dashboard "de verdade"** — não usa mais os 4 cards estáticos que só repetiam o menu lateral. Layout: cabeçalho (`ms-dash-page-header`) com boas-vindas + CRP e botão "Agendar Sessão" (abre a mesma modal `#modalNovaSessao` reaproveitada de "Minhas Sessões"/"Minha Agenda", só visível se houver paciente vinculado); 4 cards de indicadores (`ms-dash-cards`/`ms-stat-numero`) — Sessões Hoje (`Status = Agendada` no dia), Pacientes Ativos (`VinculoService.ObterPacientesAtivosAsync().Count`), Atendimentos no Mês (`Status = Realizada` no mês) e Sessões Canceladas (`Status = Cancelada` no mês), mesmas contagens já usadas em `SessoesController`/`AgendaController`; e uma tabela "Atendimentos de Hoje" (todas as sessões do dia, qualquer status, com badge `.ms-badge-*` e botão "Prontuário" para `PacientesController.Detalhes`), com empty state amigável quando não há sessão no dia.
- `DashboardController` agora também injeta `VinculoService` (mesmo padrão de `SessoesController`/`AgendaController`) só para os indicadores/modal da tela inicial — `Configuracoes`/`AtualizarPerfil`/`AtualizarPreferencias`/`AlterarSenha` continuam sem depender dele.
- `DashboardViewModel` ganhou `SessoesHoje`/`PacientesAtivos`/`AtendimentosNoMes`/`SessoesCanceladasMes`/`AtendimentosDeHoje` (reaproveita `SessaoProfissionalListItemViewModel`) e `Pacientes`/`DuracaoPadraoSessaoMinutos` (só para alimentar a partial `_ModalNovaSessao`) — tudo calculado direto em `Index()`, sem endpoint AJAX próprio (dashboard inicial é só SSR, sem paginação/filtro).

### Configurações (Profissional)
- Tela única em `Views/Dashboard/Configuracoes.cshtml` (layout `_LayoutDashboard`), com abas Bootstrap ("Perfil Profissional" / "Preferências da Clínica" / "Segurança"), mesmo padrão visual/AJAX da tela `Configuracoes` do Paciente (Fetch API por form, feedback em Bootstrap Toast, sem reload de página).
- Aba "Perfil Profissional": Nome/CRP/Telefone/Abordagem/Biografia começam `readonly`, com botão "Editar Perfil" que libera esses campos e troca pelos botões "Salvar Alterações"/"Cancelar" (`wwwroot/js/dashboard-configuracoes.js`, mesmo padrão de `wwwroot/js/painel-configuracoes.js`); "Cancelar" restaura os valores originais e volta pro modo leitura, e um salvamento bem-sucedido também volta pro modo leitura. O campo E-mail é **sempre** `readonly` (nunca entra em modo edição, mas continua sendo enviado no POST — só `readonly`, não `disabled`, porque um campo `disabled` não é incluído no `FormData` e quebraria a validação `[Required]` do e-mail no servidor) e mostra abaixo um texto explicando que ele é o login de acesso.
- `Models/Entities/Profissional.cs` ganhou `AbordagemEspecialidades` (string?, até 200 caracteres — distinto de `Apresentacao`, que continua sendo a biografia livre) e as preferências da clínica `DuracaoPadraoSessaoMinutos` (int, default 50) e `ValorPadraoConsulta` (decimal) — migration `AdicionaConfiguracoesProfissional`. Registros criados antes dessa migration ficaram com `DuracaoPadraoSessaoMinutos = 0`; `DashboardController.Configuracoes` corrige isso na leitura (exibe 50 se o valor salvo for 0), sem precisar tocar no banco.
- `Models/ViewModels/ConfiguracoesProfissionalViewModel.cs` (dados pra renderizar a tela), `AtualizarPerfilProfissionalViewModel.cs`, `AtualizarPreferenciasProfissionalViewModel.cs` e `AlterarSenhaProfissionalViewModel.cs` — um ViewModel por form, cada um postado direto como parâmetro (mesmo padrão do Cadastro/Configurações do Paciente).
- `DashboardController` (`[Authorize(Roles = AutenticacaoService.PapelProfissional)]`, já aplicado à classe):
  - `Configuracoes()` (GET) — carrega o próprio Profissional logado via `User.ObterProfissionalId()`.
  - `AtualizarPerfil(AtualizarPerfilProfissionalViewModel)` (POST, JSON) — atualiza Nome/E-mail/Telefone/CRP/Abordagem/Apresentação; revalida duplicidade de e-mail contra outros Profissionais antes de salvar.
  - `AtualizarPreferencias(AtualizarPreferenciasProfissionalViewModel)` (POST, JSON) — atualiza Duração Padrão da Sessão e Valor Padrão da Consulta (ainda não consumidos por nenhuma tela de agendamento — ficam para um card futuro).
  - `AlterarSenha(AlterarSenhaProfissionalViewModel)` (POST, JSON) — exige a senha atual e confere com `AutenticacaoService.VerificarSenha` antes de trocar o hash com `HashSenha`.
  - Todas usam `User.ObterProfissionalId()` (nunca um Id do corpo da requisição) e têm `[ValidateAntiForgeryToken]`.
- `wwwroot/js/dashboard-configuracoes.js` — um listener de `submit` por form (3 forms independentes), reaproveitando a mesma função `exibirToast` duplicada do padrão usado em `painel-configuracoes.js` (cada painel carrega seu próprio JS, sem script compartilhado entre Profissional e Paciente).

### Pacientes
- `Controllers/PacientesController.cs`: `Index` (lista os pacientes vinculados ao profissional logado), `Detalhes` (ficha do paciente), `VerificarPacienteExistente`, `Vincular`, `Criar`, `GerarNovaSenha`.
- Toda consulta/gravação revalida a posse via `VinculoService` (ver seção "Vínculo Paciente-Profissional" abaixo) — **nunca** compara um `ProfissionalId` direto na entidade `Paciente` (ela não tem mais esse campo).
- **Fluxo "Novo Paciente" em 2 passos** (modal `#modalCadastroPaciente` em `Views/Pacientes/Index.cshtml`, controlado por `wwwroot/js/pacientes.js`), pra não duplicar paciente que já existe — **o CPF é o identificador usado pra buscar/evitar duplicidade** (o e-mail é só um campo cadastral comum, não é mais usado nessa checagem):
  1. Etapa "Verificar CPF" — só um campo de CPF (obrigatório, com máscara `000.000.000-00` aplicada em JS puro, sem lib nova); `[HttpGet] VerificarPacienteExistente(cpf)` busca via `BuscarPacientePorCpfAsync` (método privado, normaliza removendo tudo que não for dígito com `CpfUtil.Normalizar`, reaproveitado por `Criar`) e retorna `{ existe, pacienteId, nomeCompleto, iniciais }`.
  2. Se `existe: true` → etapa "Paciente encontrado" (mostra iniciais/nome, sem reexibir nem sobrescrever os dados dele). Botão "Vincular este paciente" chama `[HttpPost] Vincular(pacienteId)`, que confere se já está vinculado (erro "Este paciente já está na sua lista.") e senão chama `VinculoService.CriarVinculo` — **não** gera senha nova nem mexe no cadastro do paciente.
  3. Se `existe: false` → etapa "Cadastro completo" (formulário de sempre, CPF já vem preenchido e `readonly`, com um link "trocar CPF" pra voltar à etapa 1; e-mail volta a ser um campo normal e obrigatório do formulário). Submete pro `[HttpPost] Criar` de sempre.
  - `Criar` continua existindo exatamente como antes (gera senha temporária, cria o Vínculo), mas a checagem de duplicidade por CPF no envio final passou a ser só uma **rede de segurança contra condição de corrida** — não deveria ocorrer no fluxo normal (a tela já checou o CPF na etapa 1); se ocorrer, `Criar` **não cadastra** e retorna erro pedindo pra verificar de novo (sem vincular automaticamente).
  - O modal sempre volta pra etapa 1 ao reabrir (`show.bs.modal` reseta tudo).
  - `Models/ViewModels/CpfUtil.cs` centraliza a normalização de CPF (remove tudo que não for dígito) — usado tanto por `PacientesController` quanto por `AccountController.CadastroPaciente`, pra garantir que o CPF é sempre armazenado só com dígitos, e as buscas funcionam independente de como foi digitado.
  - CPF é obrigatório em todo cadastro de Paciente (`PacienteViewModel.Cpf` e `PacienteCadastroViewModel.Cpf` com `[Required]`) — tanto no cadastro feito pelo Profissional quanto no autocadastro.
- Acesso do paciente: ao cadastrar do zero, `Criar` gera uma senha temporária aleatória (`AutenticacaoService.GerarSenhaTemporaria`), salva só o hash (`HashSenhaPaciente`) e retorna o texto puro **uma única vez** no JSON de sucesso. O front-end (`wwwroot/js/pacientes.js` + modal compartilhada `#modalSenhaTemporaria` em `_LayoutDashboard.cshtml`, via `window.exibirSenhaTemporaria`) exibe essa senha com um botão de copiar (`navigator.clipboard`) — ela nunca é armazenada em texto puro nem pode ser recuperada depois.
- Se a senha for perdida, o profissional usa o botão "Gerar nova senha" na aba Dados Cadastrais da ficha (`wwwroot/js/pacientes-detalhes.js`, action `GerarNovaSenha`), que sobrescreve o hash antigo (invalidando-o) e mostra a nova senha do mesmo jeito. Não existe fluxo de "recuperar" a senha antiga — só resetar.
- O paciente já consegue logar com essa senha em `/Account/Login` (login continua sendo por e-mail, ver seção "Autenticação" acima) e cai no seu próprio painel — ver "Painel do Paciente" abaixo. Além disso, o próprio paciente pode se autocadastrar direto — ver "Cadastro (Profissional e Paciente)" acima; `Models/ViewModels/PacienteCadastroViewModel.cs` só pede os campos essenciais (`NomeCompleto`, `Email`, `Telefone`, `DataNascimento`, `Cpf`, `Senha`/`ConfirmarSenha`) — `Sexo`, `ContatoEmergencia` e `Profissao` ficam de fora, o paciente completa depois em Configurações (ainda não implementado).

### Vínculo Paciente-Profissional
- Relação N:N via `Models/Entities/VinculoPacienteProfissional.cs` (`PacienteId`, `ProfissionalId`, `Status` — enum `StatusVinculo.Ativo`/`Encerrado`, `DataInicio`, `DataFim`) — substituiu a antiga FK direta `Paciente.ProfissionalId` (removida), permitindo que um paciente tenha vários profissionais ao longo do tempo.
- `Services/VinculoService.cs` (registrado no DI como scoped) centraliza toda a checagem de posse e criação de vínculo: `PacientePertenceAoProfissionalAsync(pacienteId, profissionalId)` (só considera vínculo com `Status = Ativo`), `ObterPacientesAtivosAsync(profissionalId)` (lista para o `Index`) e `CriarVinculo(pacienteId, profissionalId)` (usado por `PacientesController.Criar`; não chama `SaveChangesAsync` sozinho — quem chama inclui na mesma unidade de trabalho que grava o `Paciente`).
- Nenhuma controller compara `ProfissionalId` direto na entidade `Paciente` — toda checagem de posse (Pacientes e Anotações Confidenciais) passa por `VinculoService`.
- Migration `AddVinculoPacienteProfissional`: cria a tabela `Vinculos` **antes** de mexer em `Pacientes`, faz um `INSERT ... SELECT` migrando cada paciente existente para um vínculo `Ativo` (com a `DataCadastro` do paciente como `DataInicio`) e só depois remove a FK/coluna antiga — a ordem importa, senão os dados são perdidos antes de serem copiados.
- Ainda não existe tela de gerenciar vínculos (ex.: encerrar vínculo com um paciente) — fica para um card futuro.

### Painel do Paciente
- Autenticação e diferenciação de papel (Profissional vs Paciente) descritas na seção "Autenticação" acima.
- `Controllers/PainelPacienteController.cs` (`[Authorize(Roles = AutenticacaoService.PapelPaciente)]`): action `Index` — obtém o Id do paciente via `User.ObterPacienteId()`, carrega o próprio registro e exibe a tela "Início" (boas-vindas + cards placeholder).
- Layout próprio `Views/Shared/_LayoutDashboardPaciente.cshtml` — **não** reaproveita o `_LayoutDashboard.cshtml` do Profissional (layouts separados por perfil, mesmo padrão de isolamento já usado no restante do projeto), mas reaproveita `wwwroot/css/dashboard.css` e `wwwroot/js/dashboard.js` (sidebar retrátil, item ativo por rota — mesmo comportamento do dashboard do Profissional).
- Sidebar do Paciente: "Início", "Minhas Sessões" (ver seção própria abaixo), "Profissionais" (ver seção própria abaixo) e "Configurações" (ver seção própria abaixo) já são reais — não existe mais um item separado de busca no menu, a busca geral virou uma aba dentro de "Profissionais".
- Views em `Views/PainelPaciente/*`, model `Models/ViewModels/PainelPacienteViewModel.cs`.

### Profissionais (dentro do Painel do Paciente)
- Tela única com abas ("Meus Profissionais" / "Todos os Profissionais") — concentra tanto os profissionais com quem o paciente já teve/tem vínculo quanto o diretório geral de busca; **não existe mais** uma página separada de diretório/busca (a antiga tela `Diretorio.cshtml`, com cards e atalhos de `tel:`/WhatsApp, foi removida — a interface agora é só informativa, sem nenhum atalho de contato direto).
- `Models/ViewModels/PainelProfissionaisViewModel.cs`: `PainelProfissionaisViewModel` (duas listas, `MeusProfissionais`/`TodosProfissionais`, mais `AbaInicial` — `"meus"` ou `"todos"` — e `TermoBusca`, usados para abrir a aba certa já com a busca preenchida) e `ProfissionalListaItemViewModel` (item comum às duas abas — `VinculoAtivo` é `null` na aba "Todos", `true`/`false` conforme `Status` do vínculo na aba "Meus").
- `PainelPacienteController`:
  - `Profissionais(string? aba, string? busca)` (GET) — monta as duas listas via os métodos privados `ObterMeusProfissionaisAsync`/`ObterTodosProfissionaisAsync` (este reaproveitado pela busca AJAX abaixo); `aba="todos"` abre direto na aba de busca (com `busca` já aplicado ao carregar a View, sem esperar o primeiro fetch do JS).
  - `Diretorio(string? busca)` (GET) — rota antiga, mantida **só como redirect** (`RedirectToAction(nameof(Profissionais), new { aba = "todos", busca })`) para não quebrar links/favoritos existentes.
  - `BuscarProfissionais(string? busca)` (GET, JSON) — chamado via Fetch API pela aba "Todos os Profissionais" a cada tecla digitada (debounce de 300ms em `wwwroot/js/painel-profissionais.js`), mesmo padrão de `PacientesController.BuscarAnotacoes` (retorna a lista plana em JSON, o JS remonta as linhas da tabela no cliente). A aba "Meus Profissionais" não tem busca — é sempre a lista completa de vínculos do paciente.
  - `DetalhesProfissional(Guid id)` (GET, JSON) — alimenta a modal `#modalDetalhesProfissional` (nome, CRP, e-mail, telefone, apresentação, foto/iniciais); é **só leitura**, sem checagem de vínculo (mesma info já pública nas duas abas), chamada tanto pela aba "Meus" quanto "Todos".
- Modal `#modalDetalhesProfissional` (em `Views/PainelPaciente/Profissionais.cshtml`) é exclusivamente informativa: exibe biografia/apresentação e permite copiar e-mail/telefone (`navigator.clipboard`, ícone muda pra "check" por 1,5s) — **sem** nenhum botão de agendamento ou link de WhatsApp. Abre tanto pelo botão "Ver Detalhes" quanto por clicar em qualquer ponto da linha da tabela (`wwwroot/js/painel-profissionais.js`, delegação em `tr[data-profissional-id]`, ignorando cliques em botões dentro da linha).
- Coluna "Abordagem / Especialidades" reaproveita o campo único `Profissional.Apresentacao` — a entidade não tem um campo de especialidades separado; se isso mudar no futuro, atualizar `ProfissionalListaItemViewModel` e as duas queries do controller.
- Sidebar do Paciente tem só um item "Profissionais" (sem item separado de busca) — link real em `_LayoutDashboardPaciente.cshtml`.

### Sessões (dentro do Painel do Paciente)
- Entidade `Models/Entities/Sessao.cs` (`PacienteId`, `ProfissionalId`, `DataHora`, `DuracaoMinutos`, `Status` — enum `Models/Entities/StatusSessao.cs`: `Agendada`/`Realizada`/`Cancelada`; migration `AdicionaDuracaoSessao` acrescentou `DuracaoMinutos`, default `0` para registros antigos). A criação/gestão de sessões é feita pelo Profissional (ver "Sessões (Painel do Profissional)" abaixo) — a tabela `Sessoes` continua sendo só consumida (leitura) pelo Paciente nesta seção.
- `PainelPacienteController.Sessoes()` (GET) — carrega todas as sessões do paciente logado (`User.ObterPacienteId()`, nunca um Id externo) numa única consulta e monta `PainelSessoesViewModel` (`Models/ViewModels/PainelSessoesViewModel.cs`): `ProximaSessao` (próxima com `Status = Agendada` e `DataHora` futura), `TotalSessoesRealizadas`, `Agendadas` (`Status = Agendada`) e `Historico` (`Realizada`/`Cancelada`, mais recente primeiro).
- `DetalhesSessao(Guid id)` (GET, JSON) — alimenta a modal `#modalDetalhesSessao` em `Views/PainelPaciente/Sessoes.cshtml`; a query já filtra por `PacienteId == User.ObterPacienteId()` (não só por `id`), então uma sessão de outro paciente nunca é retornada. Retorna **apenas** dados administrativos (data, hora, nome do profissional, status) — a entidade `Sessao` propositalmente não tem nenhum campo de prontuário/anotação, então não há risco de vazar conteúdo confidencial por esse endpoint (esse conteúdo mora só em `AnotacaoConfidencial`, exclusivo do Profissional).
- View com dois cards de destaque (Próxima Sessão / Total de Sessões Realizadas, estilo `.ms-stat-numero`) e abas Bootstrap "Agendadas"/"Histórico" (mesmo padrão visual de `Profissionais.cshtml`), cada linha da tabela renderizada pela partial `Views/PainelPaciente/_LinhaSessao.cshtml`. Badges de status têm classes próprias em `dashboard.css`: `.ms-badge-agendada`, `.ms-badge-realizada`, `.ms-badge-cancelada`.
- `wwwroot/js/painel-sessoes.js` — só abre a modal de detalhes via Fetch API (`btn-ver-detalhes-sessao` → `DetalhesSessao`); sem paginação/busca por enquanto (volume baixo por paciente).

### Sessões (Painel do Profissional)
- Tela própria `Controllers/SessoesController.cs` (`[Authorize(Roles = AutenticacaoService.PapelProfissional)]`) + `Views/Sessoes/Index.cshtml` (layout `_LayoutDashboard`) — controller dedicado, mesmo padrão de `PacientesController` (não fica dentro do `DashboardController`), com DI de `ApplicationDbContext`, `ILogger<SessoesController>` e `VinculoService`.
- **Paginação (10 por página) + rolagem interna (~4 linhas visíveis)**, mesma regra usada nas Anotações Confidenciais: `SessoesController.ObterPaginaSessoesAsync` (privado, reaproveitado por `Index` e `BuscarSessoes`) pagina 10 sessões por vez, filtrando por `aba` (`"agendadas"` = `Status = Agendada` ordenado por `DataHora` crescente; `"historico"` = `Status != Agendada` ordenado decrescente) e, opcionalmente, por `pacienteId`. O `Index()` (GET) só busca a página 1 de cada aba (evita carregar todas as sessões do profissional de uma vez); `BuscarSessoes(string aba, int pagina, Guid? pacienteId)` (GET, JSON) é o endpoint AJAX chamado pela troca de página, pela troca de aba (a aba "Histórico" só é buscada na primeira vez que é aberta — `wwwroot/js/sessoes.js`, flag `ms_historicoCarregado`) e pelo filtro de paciente. Na View, cada tabela fica dentro de `.table-responsive.ms-dash-table-scroll` (`max-height` + `overflow-y: auto` + `thead` `position: sticky`, `dashboard.css`) e a paginação usa a classe compartilhada `.ms-pagination` (generalizada a partir do que antes era só `#paginacaoAnotacoes`, também usada em `Views/Pacientes/Detalhes.cshtml`).
- **Filtro por Paciente**: campo de busca aberto (`<input id="filtroPacienteSessoesInput">`, não mais um `<select>`) acima das abas (visível só quando há pacientes vinculados) — digitar nome ou CPF (com ou sem máscara) dispara `BuscarPacientesFiltro(termo)` (GET, JSON, debounce de 300ms em `sessoes.js`) com até 3 sugestões, buscando só entre os pacientes vinculados ao profissional (`match` por `NomeCompleto.Contains` case-insensitive ou `Cpf.Contains` usando `CpfUtil.Normalizar(termo)`, já que o CPF é salvo só com dígitos); clicar numa sugestão (`.ms-filtro-paciente-sugestao`, dropdown `#sugestoesFiltroPacienteSessoes`) preenche o campo e só então `sessoes.js` recarrega as duas abas (página 1) via `BuscarSessoes` passando `pacienteId` — editar o texto sem escolher uma sugestão nova invalida a seleção anterior (volta a listar todos os pacientes) até a próxima escolha. Botão "X" (`#btnLimparFiltroPacienteSessoes`) limpa a seleção. Estilos próprios `.ms-filtro-paciente-*` em `dashboard.css` (reaproveita só o wrapper `.ms-anotacao-toolbar`/`.ms-anotacao-search` para o ícone de funil).
- `Index()` (GET) monta `PainelSessoesProfissionalViewModel` (`Models/ViewModels/PainelSessoesProfissionalViewModel.cs`): `SessoesHoje` (`Status = Agendada` e `DataHora` no dia atual, via `CountAsync`), `ProximaSessao` (próxima `Agendada` futura), `AtendimentosNoMes` (`Status = Realizada` no mês corrente) — todos calculados como consultas agregadas separadas, não a partir da lista paginada — mais `Agendadas`/`PaginaAtualAgendadas`/`TotalPaginasAgendadas`, `Historico`/`PaginaAtualHistorico`/`TotalPaginasHistorico` (página 1 de cada) e `Pacientes` (`List<PacienteSelectItemViewModel>`, agora com `Id`/`NomeCompleto`/`CpfFormatado`, vindo de `_vinculoService.ObterPacientesAtivosAsync`; usado só pelo popup "Procurar Paciente" do modal "Nova Sessão" — o filtro de paciente busca separadamente via `BuscarPacientesFiltro`, não usa essa lista). `DuracaoPadraoSessaoMinutos` do profissional (corrigido para 50 se salvo como 0, mesma regra do `DashboardController.Configuracoes`) preenche o valor padrão do campo Duração no modal "Nova Sessão".
- **Campo Paciente do modal "Nova Sessão" — lookup, não mais `<select>`**: `#NovaSessaoPacienteBusca` (texto) + `#NovaSessaoPacienteId` (hidden, é o que é postado como `PacienteId`) reaproveitam exatamente a mesma UX/endpoint do filtro acima (`BuscarPacientesFiltro`, sugestões em `#sugestoesNovaSessaoPaciente`, mesmas classes `.ms-filtro-paciente-*`). Diferencial: um botão de lupa (`#btnProcurarNovaSessaoPaciente`) abre a modal `#modalProcurarPacienteSessao` (`.modal-dialog-scrollable`) — um lookup nos moldes Dynamics 365, para quando o profissional não lembra o nome/CPF exato — com a lista completa de `Model.Pacientes` já renderizada no servidor (`#listaModalProcurarPaciente`, um botão `.ms-lookup-paciente-item` por paciente, com `data-paciente-cpf-normalizado` pré-calculado via `CpfUtil.Normalizar`) e um campo de busca (`#buscaModalProcurarPaciente`) que filtra **no cliente** (sem round-trip, já que a lista inteira já está no DOM) por nome ou CPF a cada tecla. Clicar numa sugestão ou num item da lista do popup chama a mesma função `selecionarPaciente(id, nome)` em `nova-sessao.js`, que preenche o hidden + o texto e mostra o botão "X" (`#btnLimparNovaSessaoPaciente`) de limpar seleção. Reabrir o modal "Nova Sessão" (`show.bs.modal`) sempre limpa essa seleção junto com `formNovaSessao.reset()`. Essa lógica toda mora hoje em `wwwroot/js/nova-sessao.js` (ver "Agenda (Painel do Profissional)" abaixo pra entender por quê — a modal é compartilhada com a tela de Agenda, não é exclusiva de "Minhas Sessões").
- **Campo Data e Hora (Nova Sessão e Editar Sessão) — `type="date"` + `type="time"` separados, não `datetime-local`**: o widget nativo de `datetime-local` do Chrome não fecha sozinho ao escolher só a data (fica esperando a hora também ser preenchida no mesmo popup), o que confundia o profissional. Cada modal tem `#NovaSessaoData`/`#NovaSessaoHora` (ou `#EditarSessaoData`/`#EditarSessaoHora`) — inputs nativos que fecham o próprio popup ao serem preenchidos — mais um `<input type="hidden" name="DataHora">` que é o campo realmente postado (o `NovaSessaoViewModel`/`AtualizarSessaoViewModel` continuam recebendo um único `DateTime`). `combinarDataHora`/`separarDataHora`/`configurarSeletorHora` (formato `yyyy-MM-ddTHH:mm`, igual ao que `datetime-local` já usava) vivem em `wwwroot/js/sessao-utils.js` — helpers globais compartilhados por `sessoes.js` (Editar Sessão) e `nova-sessao.js` (Nova Sessão), chamados nos eventos `change` de cada campo, de novo no `submit` de cada form (rede de segurança) e em `preencherModalEdicao` (Editar Sessão, a partir do `dataHoraIso` vindo de `BuscarSessoes`/`ObterSessao`/`data-sessao-data`).
- `Criar(NovaSessaoViewModel)` (POST, JSON) — valida que o paciente pertence ao profissional logado via `_vinculoService.PacientePertenceAoProfissionalAsync` antes de gravar; `Status` chega como string do `<select>` e é convertido para `StatusSessao` com `Enum.TryParse`. Após sucesso, a tela recarrega inteira (`window.location.reload()`) — mantém simples porque os cards de estatística/filtro/paginação também precisam recalcular.
- `Atualizar(AtualizarSessaoViewModel)` (POST, JSON) — busca a sessão filtrando por `Id` **e** `ProfissionalId == User.ObterProfissionalId()` (nunca só por `Id`), atualiza `DataHora`/`DuracaoMinutos`/`Status`; mesmo comportamento de recarregar a página após sucesso.
- `ObterSessao(Guid id)` (GET, JSON) — único endpoint de leitura individual da sessão; usado tanto pelo fluxo "Abrir" vindo da Ficha do Paciente quanto por **todo** clique em "Status/Editar" (`.btn-editar-sessao`) — a modal "Editar Sessão" sempre busca os dados atualizados via `abrirModalEdicao(sessaoId)` em `sessoes.js`, sem mais depender só dos `data-*` do botão da linha (que continuam existindo em `_LinhaSessao.cshtml`/`construirLinhaSessao`, mas não são mais usados para preencher esta modal). Retorna, além dos campos já existentes, `pacienteId` e `anotacoesClinicas`.
- **Anotações Clínicas / Evolução da Sessão**: `Sessao.AnotacoesClinicas` (string?, migration `AdicionaEvolucaoECombinadosSessao`) — texto livre confidencial do profissional, editado no textarea `#EditarSessaoAnotacoesClinicas` dentro do próprio `formEditarSessao` (é salvo junto com Data/Duração/Status no submit normal de "Salvar Alterações", via `AtualizarSessaoViewModel.AnotacoesClinicas` → `SessoesController.Atualizar`).
- View: cards de destaque "Sessões Hoje" / "Próxima Sessão" / "Atendimentos no Mês" (`.ms-stat-numero`), botão "Nova Sessão" (abre `#modalNovaSessao`, oculto se o profissional não tiver nenhum paciente vinculado), abas Bootstrap "Hoje / Agendadas" (padrão) e "Histórico / Concluídas". Cada linha (partial `Views/Sessoes/_LinhaSessao.cshtml`, reaproveitando os badges `.ms-badge-agendada/realizada/cancelada`) tem dois botões: "Status/Editar" (abre `#modalEditarSessao`) e "Prontuário" (link direto para `PacientesController.Detalhes`, a ficha do paciente).
- `wwwroot/js/sessoes.js` — mistura SSR + AJAX: a página 1 de "Agendadas" vem pronta do Razor (evita um round-trip no primeiro carregamento); toda paginação, troca de aba/filtro e o fluxo "Abrir" (abaixo) passam por `BuscarSessoes`/`ObterSessao` e reconstroem as linhas via `construirLinhaSessao` (JS), espelhando a marcação de `_LinhaSessao.cshtml` — **se a partial mudar, replicar a mudança nessa função**. Um listener de `submit` no form Editar Sessão, com `FormData` + token antiforgery (`@Html.AntiForgeryToken()`) e toast próprio (`#toastSessoes`) — o submit da Nova Sessão mora em `nova-sessao.js`, não aqui (ver "Agenda (Painel do Profissional)" abaixo).
- Entidade `Sessao` ganhou `DuracaoMinutos` (int, por sessão) na migration `AdicionaDuracaoSessao` — distinto de `Profissional.DuracaoPadraoSessaoMinutos`, que é só o valor sugerido ao abrir o modal "Nova Sessão".
- Item "Minhas Sessões" do menu lateral do Profissional (`Views/Shared/_LayoutDashboard.cshtml`) aponta para `asp-controller="Sessoes" asp-action="Index"`.
- A aba "Histórico de Sessões" da Ficha do Paciente (`Views/Pacientes/Detalhes.cshtml`, acessada pelo botão "Prontuário" nas linhas de `Views/Sessoes/Index.cshtml`) era um placeholder estático ("Em breve...") e foi conectada aos dados reais: `PacientesController.Detalhes` agora também carrega `Sessoes` (`List<SessaoProfissionalListItemViewModel>`, reaproveitado de `PainelSessoesProfissionalViewModel.cs`) filtrando por `PacienteId` **e** `ProfissionalId == User.ObterProfissionalId()`, ordenado por `DataHora` decrescente — a projeção também traz `AnotacoesClinicas` de cada sessão, único lugar do código que popula esse campo do ViewModel (em `SessoesController`, fica com valor default/vazio — não é necessário nas telas de lá). A tabela na aba é só leitura (Data e Hora/Duração/Status, mesmos badges `.ms-badge-*`), sem paginação própria (volume por paciente é baixo). Cada linha tem um botão **"Abrir"** (`asp-route-abrirSessaoId="@sessao.Id"`) que navega para `Sessoes/Index?abrirSessaoId={id}` (lá, `sessoes.js` lê esse parâmetro da URL e já abre `#modalEditarSessao` preenchida via `ObterSessao`) e, só quando a sessão tem `AnotacoesClinicas`, um botão **"Evolução"** que expande (`data-bs-toggle="collapse"`) uma linha logo abaixo (`.ms-sessao-evolucao`) com o texto das anotações clínicas — tudo renderizado no servidor, sem AJAX.

### Agenda (Painel do Profissional) — V0
- Tela própria `Controllers/AgendaController.cs` (`[Authorize(Roles = AutenticacaoService.PapelProfissional)]`) + `Views/Agenda/Index.cshtml` (layout `_LayoutDashboard`) — controller dedicado, mesmo padrão de `SessoesController`/`PacientesController` (não fica dentro do `DashboardController`).
- **Grade semanal/mensal é implementada em HTML/CSS/JS puro (`wwwroot/js/agenda.js`) — não usa nenhuma biblioteca de calendário (ex.: FullCalendar)**, para não introduzir uma dependência externa nova; a "grade de rastreabilidade visual" é uma grid CSS de 7 colunas (semana, `.ms-agenda-grade-semana`) ou 7 colunas x N semanas (mês, `.ms-agenda-grade-mes`, incluindo dias de padding do mês anterior/seguinte para fechar semanas completas), com um botão `.ms-agenda-chip` por sessão dentro de cada dia. Alternar entre as visões (`#btnAgendaVisaoSemana`/`#btnAgendaVisaoMes`) e navegar (`#btnAgendaAnterior`/`#btnAgendaProximo`/`#btnAgendaHoje`) só troca a data de referência (`ms_agendaReferencia`, em memória no JS) e busca de novo — não há estado persistido entre navegações de página.
- `AgendaController.Index()` (GET) monta `PainelAgendaProfissionalViewModel` (`Models/ViewModels/PainelAgendaProfissionalViewModel.cs`) com só os 3 contadores dos cards do topo — a grade em si **não** vem do SSR, é buscada pelo JS assim que a página carrega (`carregarAgenda()` no final de `agenda.js`), evitando duplicar a lógica de montagem de chip em Razor e em JS (diferente do padrão SSR+AJAX usado em `Sessões`, aqui simplificado de propósito por ser uma V0).
  - `SessoesSemana`: **janela móvel dos últimos 7 dias** (`hoje.AddDays(-6)` até `hoje`, não a semana de calendário que aparece na grade, que o profissional navega livremente) — conta só `Agendada`/`Realizada` (`Cancelada` não ocupa agenda de fato).
  - `SessoesMes`: contagem de **todas** as sessões (qualquer `Status`) com `DataHora` no mês corrente — é o volume total; o card "Sessões Canceladas" ao lado é o recorte específico de cancelamentos, não uma subtração do total.
  - `SessoesCanceladasMes`: `Status == Cancelada` no mês corrente.
- `BuscarSessoesAgenda(DateTime inicio, DateTime fim)` (GET, JSON) — único endpoint de dados da grade, filtra sempre por `ProfissionalId == User.ObterProfissionalId()` e por `DataHora >= inicio && DataHora < fim` (intervalo half-open); é o mesmo endpoint pras duas visões — quem calcula o intervalo certo (incluindo os dias de padding do mês) é o cliente (`agenda.js`, `inicioGradeMes`/`fimGradeMesExclusivo` vs. `inicioDaSemana`).
- Cores dos `.ms-agenda-chip`/`.ms-agenda-dot` (Agendada azul, Realizada verde, Cancelada cinza) são **classes próprias da Agenda**, não os badges `.ms-badge-agendada/realizada/cancelada` já usados em Sessões/Ficha do Paciente (que são laranja/verde/vermelho) — foi um pedido explícito para esta tela especificamente; a modal de detalhes (`#modalDetalhesSessaoAgenda`), por outro lado, reaproveita os badges `.ms-badge-*` de sempre, então a mesma sessão pode aparecer com uma cor na grade e outra na modal — intencional, não inconsistência a corrigir.
- Clicar num `.ms-agenda-chip` abre a modal `#modalDetalhesSessaoAgenda` (Paciente/Data e Hora/Duração/Status + botão "Acessar Prontuário" para `/Pacientes/Detalhes/{pacienteId}`) preenchida direto pelos `data-*` do botão — sem round-trip ao servidor, mesmo padrão já usado em `sessoes.js` para o clique numa linha da tabela.
- Item "Minha Agenda" do menu lateral do Profissional (`Views/Shared/_LayoutDashboard.cshtml`) aponta para `asp-controller="Agenda" asp-action="Index"` (antes era um link `href="#"` estático).
- **Criar sessão a partir de um dia da grade**: clicar num dia (fora de um `.ms-agenda-chip`, que abre a modal de detalhes) abre a modal "Nova Sessão" **direto na própria tela da Agenda**, sem navegar para "Minhas Sessões" — `agenda.js` chama `window.MsNovaSessao.abrirComData(dataIso)`, que preenche o campo Data e mostra a modal (a Hora fica em aberto, para o profissional escolher). Editar uma sessão existente pela Agenda continua sendo só leitura (modal de detalhes com link "Acessar Prontuário") — a edição de fato só existe em "Minhas Sessões".
- **A modal "Nova Sessão" é compartilhada entre "Minhas Sessões" e "Minha Agenda"**, não duplicada: `Views/Shared/_ModalNovaSessao.cshtml` (parâmetro `Models/ViewModels/NovaSessaoModalViewModel.cs` — só `Pacientes`/`DuracaoPadraoSessaoMinutos`) contém o HTML da modal "Nova Sessão" **e** da popup "Procurar Paciente"; `wwwroot/js/nova-sessao.js` contém toda a lógica (lookup de paciente, seletor de hora, submit pro `/Sessoes/Criar`, toast próprio `#toastNovaSessao`) e expõe `window.MsNovaSessao.abrirComData(dataIso)` pra quem precisar abrir a modal já preenchida sem round-trip. Tanto `Views/Sessoes/Index.cshtml` quanto `Views/Agenda/Index.cshtml` incluem a partial e carregam os scripts nesta ordem: `sessao-utils.js` → `nova-sessao.js` → o script da própria página (`sessoes.js`/`agenda.js`) — a ordem importa porque `nova-sessao.js` usa os helpers globais de `sessao-utils.js` (`combinarDataHora`, `separarDataHora`, `configurarSeletorHora`, `escaparHtml`, antes definidos dentro de `sessoes.js`) e `agenda.js` usa `window.MsNovaSessao`, que só existe depois que `nova-sessao.js` roda. **Se o formulário da Nova Sessão mudar (novo campo, validação etc.), mexer só na partial/`nova-sessao.js` — nunca duplicar de volta pra `Sessoes/Index.cshtml` ou `Agenda/Index.cshtml`.** Após criar com sucesso, a modal recarrega a página atual (`window.location.reload()`) — funciona igual nas duas telas, sem precisar saber em qual delas está.
- `AgendaController` também injeta `VinculoService` (igual `SessoesController`) só pra montar `Pacientes`/`DuracaoPadraoSessaoMinutos` do `PainelAgendaProfissionalViewModel`, usados exclusivamente pela partial `_ModalNovaSessao` — a grade em si não depende dessa lista.
- Fica para um card futuro: paginação/limite de sessões por dia na grade (hoje mostra todas, sem cortar).

### Configurações (dentro do Painel do Paciente)
- Tela com abas ("Meus Dados" / "Segurança"), ambas com submit via Fetch API e feedback em Bootstrap Toast (`#toastConfiguracoes` em `Views/PainelPaciente/Configuracoes.cshtml`, cores `text-bg-success`/`text-bg-danger` do próprio Bootstrap — sem CSS customizado) — não recarrega a página em nenhum dos dois casos.
- Aba "Meus Dados": Nome/E-mail/Telefone começam `readonly`, com botão "Editar Dados" que libera os três campos e troca pelos botões "Salvar Alterações"/"Cancelar" (`wwwroot/js/painel-configuracoes.js`); "Cancelar" restaura os valores originais e volta pro modo leitura, e um salvamento bem-sucedido também volta pro modo leitura (guardando os novos valores como "originais"). O campo CPF é **sempre** `readonly`/`disabled` (nunca entra em modo edição) e mostra abaixo um texto explicando que a correção precisa ser pedida ao profissional responsável.
- `Models/ViewModels/ConfiguracoesPacienteViewModel.cs` (dados pra renderizar a tela, incluindo `CpfFormatado` via `CpfUtil.Formatar`), `AtualizarDadosPacienteViewModel.cs` e `AlterarSenhaPacienteViewModel.cs` (um por form, cada um postado direto como parâmetro — mesmo padrão do Cadastro).
- `PainelPacienteController`:
  - `Configuracoes()` (GET) — carrega o próprio Paciente logado.
  - `AtualizarDados(AtualizarDadosPacienteViewModel)` (POST, JSON) — atualiza Nome/E-mail/Telefone; **CPF nunca é aceito aqui** (nem está no ViewModel — o campo na tela é só `readonly`/`disabled`, então nem é enviado no POST). Revalida duplicidade de e-mail contra outros Pacientes antes de salvar (mesmo padrão do `CadastroPaciente`).
  - `AlterarSenha(AlterarSenhaPacienteViewModel)` (POST, JSON) — exige a senha atual e confere com `AutenticacaoService.VerificarSenhaPaciente` antes de trocar o hash com `HashSenhaPaciente`; erra com mensagem genérica ("senha atual incorreta") sem revelar mais detalhes.
  - Ambas as actions usam `User.ObterPacienteId()` (nunca um Id do corpo da requisição) e têm `[ValidateAntiForgeryToken]`, igual todo `[HttpPost]` do projeto.
- `wwwroot/js/painel-configuracoes.js` — um listener de `submit` por form, com `FormData` (inclui o token antifalsificação automaticamente) e a função `exibirToast(mensagem, sucesso)` compartilhada pelos dois.

### Badge "Alfa"
- `Views/Shared/_BadgeAlfa.cshtml` — partial isolado, incluído ao lado do logo em `Views/Home/Index.cshtml`, `_LayoutDashboard.cshtml` e `_LayoutDashboardPaciente.cshtml`.
- Feito para ser descartado facilmente quando o projeto sair da fase Alfa: basta apagar o arquivo e as 3 referências `<partial name="_BadgeAlfa" />`.

### Anotações Confidenciais (dentro da Ficha do Paciente)
- Entidade `Models/Entities/AnotacaoConfidencial.cs`, vinculada a `Paciente` e `Profissional`.
- CRUD completo via AJAX, sem reload de página: `SalvarAnotacao`, `AtualizarAnotacao`, `ExcluirAnotacao`, `BuscarAnotacoes` (paginação de 10 por página + busca por título + ordenação por data), `SugerirTitulosAnotacao` (autocomplete, sempre as 3 mais recentes que dão match).
- Timeline visual com scroll interno (`.ms-timeline-scroll`, ~4 itens visíveis por vez) e paginação Bootstrap abaixo.
- Toda action verifica que a anotação/paciente pertence ao profissional logado antes de ler ou gravar.

### Rodapé Global (Copyright/LGPD)
- `<footer class="ms-copyright-footer">` presente nos 4 layouts do projeto (`_Layout.cshtml`, `_LayoutAuth.cshtml`, `_LayoutDashboard.cshtml`, `_LayoutDashboardPaciente.cshtml`), com o texto de copyright e o aviso de conformidade com a LGPD.
- Estilo (`.ms-copyright-footer`/`.ms-copyright-footer-inner`) duplicado em `site.css`, `auth.css` e `dashboard.css` (cada layout carrega sua própria folha de estilos, não há CSS compartilhado entre eles) — se o texto/estilo mudar, replicar nos três arquivos.
- Fixado na parte inferior mesmo em páginas com pouco conteúdo via `margin-top: auto` no footer dentro de um container pai `flex-direction: column` com `min-height: 100vh` (`body` em `_Layout`/`_LayoutAuth`, `.ms-dash-main` nos layouts de dashboard) — não é `position: fixed`, então nunca sobrepõe conteúdo.

### Teste durante desenvolvimento
- NÃO utilize ferramentas de browser/headless/screenshot para validação visual.
- A validação de código deve ser feita EXCLUSIVAMENTE via `dotnet build` e testes de código.
- Deixe o teste de interface/UI no navegador sob responsabilidade do usuário.